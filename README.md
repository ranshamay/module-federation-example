# installation

- Run `npm install`

# running demo

### for csr example:

- Run `npm run start:csr`

### for ssr + csr example:

- Run `npm run start:csrssr`

### open browser at [localhost:3004](http://localhost:3004/)
